

sum = 0

for a in xrange(1,21):
    for b in xrange(1,a + 1):
        for c in xrange(1,b + 1):
            for d in xrange(1,c + 1):
                for e in xrange(1,d + 1):
                    for f in xrange(1,e + 1):
                        for g in xrange(1,f + 1):
                            for h in xrange(1,g + 1):
                                for i in xrange(1,h + 1):
                                    for j in xrange(1,i + 1):
                                        for k in xrange(1,j + 1):
                                            for l in xrange(1,k + 1):
                                                for m in xrange(1,l + 1):
                                                    for n in xrange(1,m + 1):
                                                        for o in xrange(1,n + 1):
                                                            for p in xrange(1,o + 1):
                                                                sum += p

print p